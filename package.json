{
  "name": "showdown",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build:val": "node scripts/sync-val.mjs",
    "check:val": "node scripts/sync-val.mjs --check",
    "deploy:val": "node scripts/deploy-val.mjs"
  },
  "devDependencies": {
    "@valtown/sdk": "^0.35.0",
    "@vitest/coverage-v8": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
